/**
 * Registers the Workbox-generated service worker (via vite-plugin-pwa) so the
 * app shell, bundle, sprites, and audio assets are precached for offline play.
 *
 * STUB: real registration/update-prompt wiring is owned by the PWA feature
 * assignment. This placeholder intentionally throws so any caller integrated
 * before that assignment lands fails loudly instead of silently no-op-ing.
 */
export function registerServiceWorker(): void {
  throw new Error('not implemented');
}
