# ASSIGN-019 Notes

Implemented `FruitManager.ts` as a standalone, self-contained module under
`src/engine/fruit/`. It is not wired into `GameLoop.ts` because
`GameLoop.ts` (MOD-GAMELOOP) and `levelConfig.ts` (MOD-LEVEL-CONFIG) are
modules owned by other assignments (import-only per the repo contract) and
neither file exists in the workspace yet. The fruit spawn-trigger logic
(dot-count thresholds at ~70/~170) and the level -> fruit type/points
mapping table required by US-013 are fully implemented and unit-tested
here; the GameLoop-owning assignment can import `FruitManager`,
`getFruitConfigForLevel`, and `FRUIT_SPAWN_DOT_THRESHOLDS` directly.

Note: shell/test execution was unavailable in this sandbox
(`SHELL_ALLOW_HOST=false`), so tests were verified by careful manual
review rather than an executed run.
